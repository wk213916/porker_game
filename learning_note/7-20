崎村さん、杉浦さんからのフィードバック

Rspec
let
初期値nilの設定、なくても動くよ
カードオブジェクトの名前、もっとシンプルでいい

どこをletの変数になるかを考える

全部書き直さないといけなくなる書き方なので、それはよろしくない

書き直さないようにするには、上でHandごと定義して、それを取り回して使う方法にする

let(:で一番最初に定義する箇所、必ず、何らかのインスタンスを作成する必要がある
